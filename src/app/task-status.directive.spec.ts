import { TaskStatusDirective } from './task-status.directive';

describe('TaskStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskStatusDirective();
    expect(directive).toBeTruthy();
  });
});
